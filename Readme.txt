A store inventory tracking application created using Angular + .NET Core

Git - 
gh auth login				# login to GitHub cli
gh repo create				# to create repo

cd /path/to/GadgetShop
git init					# to initialise local git repo
git add .					# stage & commit files
git commit -m "Initial commit"

git remote add origin https://github.com/your-username/GadgetShop.git		# connect to remote repo
git branch -M main			# rename branch to main
git push -u origin main			# push to main branch
